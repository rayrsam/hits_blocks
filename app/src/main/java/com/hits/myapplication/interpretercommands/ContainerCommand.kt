package com.hits.myapplication.interpretercommands

open class ContainerCommand(val queue: MutableList<BlockCommand>) : BlockCommand {
    override fun runCommand() {
        TODO("Not yet implemented")
    }
}